# comfy-audio-duration
A custom node for ComfyUI that calculates and outputs the duration of an audio file in seconds and minutes. It is flexible, accepting audio via a file path or directly from an upstream node's audio object.
